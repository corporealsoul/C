#include <stdio.h>

int main()
{
printf("\n");
	int bottom_line = 3, above_line = 20;
	int loop;

	for (loop = bottom_line; loop <= above_line; loop++)
	{
		for (size_t i = 0; i < count; i++)
		{
			/* code */
		}
		
	}
	
	
printf("\n");
return 0;
}
