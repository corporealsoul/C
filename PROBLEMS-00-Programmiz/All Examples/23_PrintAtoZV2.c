#include <stdio.h>

int main()
{
printf("\n\n");

	char loop;

	for (loop = 'A'; loop <= 'Z'; loop++)
	{
		printf("%c ", loop);
	}
	printf("\n");

	for (loop = 'a'; loop <= 'z'; loop++)
	{
		printf("%c ", loop);
	}

printf("\n\n");
return 0;
}