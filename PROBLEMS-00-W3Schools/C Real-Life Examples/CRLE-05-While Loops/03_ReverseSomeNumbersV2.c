#include<stdio.h>

int main()
{
printf("\n\n");
    // A variable with some specific numbers
    int numbers = 10;

    while (numbers) {
        printf("The number is: %d\n", numbers);
        numbers = numbers - 1;
    }

printf("\n\n");
return 0;
}