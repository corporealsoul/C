#include <stdio.h>

int main() {
printf("\n");

    size_t size_of_int = sizeof(int);  // `size_of_int` is of type `size_t`
    printf("Size of int: %zu bytes\n", size_of_int);

printf("\n");
return 0;
}
