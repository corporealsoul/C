#include <stdio.h>

int main() {

    int integer_variable_one;
    int loop = 0, count = 20;

printf("\n");

    while (loop < count)
    {
        if (loop > 10)
        {
            printf("I'm  %d - and I'm a Natural Number ! \n", loop);
        }
        loop ++;
    }
    
printf("\n");
return 0;
}