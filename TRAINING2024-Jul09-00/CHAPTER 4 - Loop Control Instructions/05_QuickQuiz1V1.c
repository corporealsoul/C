#include <stdio.h>

int main() {

    int integer_variable_one;
    int loop = 10, count = 20;

printf("\n");

    while (loop < count)
    {
        printf("I'm  %d - and I'm a Natural Number ! \n", loop);
        loop ++;
    }
    
printf("\n");
return 0;
}