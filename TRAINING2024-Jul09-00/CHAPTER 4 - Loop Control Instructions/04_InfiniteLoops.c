#include <stdio.h>

int main() {

    int integer_variable_one;
    int loop = 0, count = 10;

printf("\n");

    while (loop < count)
    {
        printf("World of C Language, where I'm in LOOOOOOP ! %d\n", loop);
        loop --;
    }
    
printf("\n");
return 0;
}